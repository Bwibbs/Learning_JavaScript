const robot = { //robot object that has the properties model, and energyLevel.  Also has provideInfo method
  model: '1E78V2',
  energyLevel: 100,
  provideInfo(){
    return `I am ` + this.model + ` and my current energy level is ` + this.energyLevel+ ' .';
  }
};
console.log(robot.provideInfo()); //print out "I am 1E78V2 and my current energy level is 100."
